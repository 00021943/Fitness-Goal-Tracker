<%- include('../partials/header') %>

    <div class="disclaimer-banner">
        <p>This web application was created to fulfill Web Technology module's requirements and does not represent an
            actual company or service</p>
    </div>

    <div class="landing-hero">
        <div class="hero-content">
            <h1>Fitness Goal Tracker</h1>
            <p class="hero-subtitle">Track your fitness goals and achieve results</p>

            <div class="hero-actions">
                <% if (isAuthenticated) { %>
                    <a href="/dashboard" class="btn btn-primary btn-lg">Go to Dashboard</a>
                    <% } else { %>
                        <a href="/auth/login" class="btn btn-primary btn-lg">Login</a>
                        <a href="/auth/register" class="btn btn-secondary btn-lg">Register</a>
                        <% } %>
            </div>
        </div>
    </div>

    <div class="features-section">
        <div class="container">
            <h2 class="section-title">App Features</h2>

            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">📊</div>
                    <h3>Progress Tracking</h3>
                    <p>Monitor your progress in real-time with visual indicators</p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">🏋️</div>
                    <h3>Various Goal Types</h3>
                    <p>Create goals for weight loss, running, strength training, and more</p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">⏰</div>
                    <h3>Deadlines & Reminders</h3>
                    <p>Set deadlines for your goals and get notifications about approaching deadlines</p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">📱</div>
                    <h3>Access from Any Device</h3>
                    <p>Use the app on your computer, tablet, or smartphone</p>
                </div>
            </div>
        </div>
    </div>

    <div class="cta-section">
        <div class="container">
            <h2>Start tracking your fitness goals today</h2>
            <p>Join thousands of users who are already achieving their goals with our app</p>

            <% if (!isAuthenticated) { %>
                <a href="/auth/register" class="btn btn-primary btn-lg">Register for Free</a>
                <% } else { %>
                    <a href="/dashboard" class="btn btn-primary btn-lg">Go to Dashboard</a>
                    <% } %>
        </div>
    </div>

    <%- include('../partials/footer') %>